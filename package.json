{
  "name": "elm-3d-playground-exploration",
  "author": "",
  "description": "",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "postinstall": "elm-tooling install",
    "prebuild": "elm-tooling install",
    "watch-tailwind": "npx tailwindcss -i ./playground-exploration/src/style.css -o ./build/output.css --watch",
    "watch-elm": "npx elm-watch hot",
    "serve": "npx http-server .",
    "dev": "bash ./scripts/prebuild.sh && npx run-pty % npm run watch-tailwind % npm run watch-elm % npm run serve % open http://127.0.0.1:8080/build/WebPage/index.html",
    "build-tailwind": "npx tailwindcss -i ./playground-exploration/src/style.css -o ./build/output.css",
    "build-elm": "npx elm-watch make --optimize",
    "build": "bash ./scripts/prebuild.sh && npm run build-tailwind && npm run build-elm",
    "deploy": "npm run build && gh-pages -d build && open https://erkal.github.io/elm-3d-playground-exploration/WebPage/index.html"
  },
  "devDependencies": {
    "elm-tooling": "^1.14.0",
    "elm-watch": "^1.1.2",
    "gh-pages": "^5.0.0",
    "http-server": "^14.1.1",
    "prettier": "^2.8.8",
    "run-pty": "^4.0.3",
    "tailwindcss": "^3.3.2"
  }
}
